# Traffic Sign Recognition using CNN

A pipeline for traffic sign classification with Convolutional Neural Networks using TensorFlow.

## Instructions

1. Download and prepare your dataset under `data/train/` and `data/test/` (see GTSRB dataset on Kaggle or official site).
2. Install dependencies: `pip install -r requirements.txt`
3. Run training and evaluation: `python main.py`
